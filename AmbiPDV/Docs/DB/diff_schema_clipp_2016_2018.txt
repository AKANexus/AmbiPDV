Legenda:
	- "+++": itens adicionados na versão mais recente;
	- "=!=": itens alterados;

CREATE TABLE TB_CIDADE_SIS
(
  ID_CIDADE Char(7) NOT NULL,
  NOME Varchar(30) CHARACTER SET WIN1252 NOT NULL COLLATE WIN_PTBR,
  SIGLA_UF Char(2) CHARACTER SET WIN1252 NOT NULL COLLATE WIN_PTBR,
  DDD Char(2) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  CODIGO_SIAFI Varchar(4), +++
  CODIGO_SEDETEC Varchar(7), +++
  CONSTRAINT TB_UF_PK PRIMARY KEY (ID_CIDADE)
);

CREATE TABLE TB_CLIENTE
(
  ID_CLIENTE Integer NOT NULL,
  ID_CONVENIO Smallint,
  DT_CADASTRO Date DEFAULT CURRENT_DATE,
  NOME Varchar(60) CHARACTER SET WIN1252 NOT NULL COLLATE WIN_PTBR,
  END_CEP Char(9) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  END_TIPO Varchar(15) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  END_NUMERO Varchar(5) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  END_LOGRAD Varchar(40) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  END_BAIRRO Varchar(35) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  END_COMPLE Varchar(15) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  DT_PRICOMP Date,
  DT_ULTCOMP Date,
  CONTATO Varchar(35) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  STATUS Char(1) CHARACTER SET WIN1252 DEFAULT 'A' COLLATE WIN_PTBR,
  LIMITE Numeric(18,2),
  DDD_RESID Char(2) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  FONE_RESID Varchar(13) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  DDD_COMER Char(2) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  FONE_COMER Varchar(13) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  DDD_CELUL Char(2) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  FONE_CELUL Varchar(13) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  DDD_FAX Char(2) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  FONE_FAX Varchar(13) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  EMAIL_CONT Varchar(50) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  EMAIL_NFE Varchar(50) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  ID_CIDADE Char(7),
  ID_TIPO Smallint,
  ID_FUNCIONARIO Smallint,
  ID_PAIS Char(4) NOT NULL,
  MENSAGEM Varchar(100) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  ID_RAMO Smallint,
  EMAIL_ADIC Varchar(50) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  OBSERVACAO Blob sub_type 1 CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  DT_MELHOR_VENCTO Smallint, +++
  CONSTRAINT TB_CLIENTES_PK PRIMARY KEY (ID_CLIENTE)
);

CREATE TABLE TB_CONTA_RECEBER
(
  ID_CTAREC Integer NOT NULL,
  DOCUMENTO Varchar(12) CHARACTER SET WIN1252 NOT NULL COLLATE WIN_PTBR,
  HISTORICO Varchar(50) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  DT_EMISSAO Date NOT NULL,
  DT_VENCTO Date NOT NULL,
  VLR_CTAREC Numeric(18,4) NOT NULL,
  TIP_CTAREC Char(1) CHARACTER SET WIN1252 NOT NULL COLLATE WIN_PTBR,
  ID_PORTADOR Integer NOT NULL,
  ID_CLIENTE Integer NOT NULL,
  INV_REFERENCIA Varchar(18),
  DT_VENCTO_ORIG Date, +++
  NSU_CARTAO Varchar(32) CHARACTER SET WIN1252 COLLATE WIN_PTBR, =!= NSU_CARTAO Varchar(12),
  CONSTRAINT TB_CONTA_RECEBER_PK PRIMARY KEY (ID_CTAREC),
  CONSTRAINT UNK_CTAREC_INV_REFERENCIA UNIQUE (INV_REFERENCIA)
);

CREATE TABLE TB_EMITENTE
(
  NOME Varchar(60) CHARACTER SET WIN1252 NOT NULL COLLATE WIN_PTBR,
  NOME_FANTA Varchar(40) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  CONTATO Varchar(35) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  END_CEP Char(9) CHARACTER SET WIN1252 NOT NULL COLLATE WIN_PTBR,
  END_TIPO Varchar(15) CHARACTER SET WIN1252 NOT NULL COLLATE WIN_PTBR,
  END_LOGRAD Varchar(40) CHARACTER SET WIN1252 NOT NULL COLLATE WIN_PTBR,
  END_NUMERO Varchar(5) CHARACTER SET WIN1252 NOT NULL COLLATE WIN_PTBR,
  END_COMPLE Varchar(15) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  END_BAIRRO Varchar(35) CHARACTER SET WIN1252 NOT NULL COLLATE WIN_PTBR,
  CNPJ Varchar(18) CHARACTER SET WIN1252 NOT NULL COLLATE WIN_PTBR,
  INSC_ESTAD Varchar(16) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  INSC_MUNIC Varchar(16) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  DDD_COMER Char(2) CHARACTER SET WIN1252 NOT NULL COLLATE WIN_PTBR,
  FONE_COMER Varchar(13) CHARACTER SET WIN1252 NOT NULL COLLATE WIN_PTBR,
  DDD_FAX Char(2) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  FONE_FAX Varchar(13) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  DDD_CELUL Char(2) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  FONE_CELUL Varchar(13) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  EMAIL_CONT Varchar(50) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  SITE Varchar(50) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  CNAE Varchar(17) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  SIMPLES Char(1) CHARACTER SET WIN1252 DEFAULT 'N' NOT NULL COLLATE WIN_PTBR,
  ID_CIDADE Char(7) NOT NULL,
  ID_RAMO Smallint NOT NULL,
  DT_COMPRA Date,
  LOGO Blob sub_type 0,
  IE_ST_AC Varchar(16),
  IE_ST_AL Varchar(16),
  IE_ST_AM Varchar(16),
  IE_ST_AP Varchar(16),
  IE_ST_BA Varchar(16),
  IE_ST_CE Varchar(16),
  IE_ST_DF Varchar(16),
  IE_ST_ES Varchar(16),
  IE_ST_GO Varchar(16),
  IE_ST_MA Varchar(16),
  IE_ST_MG Varchar(16),
  IE_ST_MS Varchar(16),
  IE_ST_MT Varchar(16),
  IE_ST_PA Varchar(16),
  IE_ST_PB Varchar(16),
  IE_ST_PE Varchar(16),
  IE_ST_PI Varchar(16),
  IE_ST_PR Varchar(16),
  IE_ST_RJ Varchar(16),
  IE_ST_RN Varchar(16),
  IE_ST_RO Varchar(16),
  IE_ST_RR Varchar(16),
  IE_ST_RS Varchar(16),
  IE_ST_SC Varchar(16),
  IE_ST_SE Varchar(16),
  IE_ST_SP Varchar(16),
  IE_ST_TO Varchar(16),
  TEXTO_COBRANCA Blob sub_type 1,
  TEXTO_COBRANCA_RODAPE Blob sub_type 1 CHARACTER SET WIN1252 COLLATE WIN_PTBR, +++
  TEXTO_COBRANCA_ASSUNTO Varchar(150) CHARACTER SET WIN1252 COLLATE WIN_PTBR +++
);

CREATE TABLE TB_ESTOQUE
(
  ID_ESTOQUE Integer NOT NULL,
  ID_GRUPO Integer,
  DESCRICAO Varchar(50) CHARACTER SET WIN1252 NOT NULL COLLATE WIN_PTBR,
  STATUS Varchar(1) CHARACTER SET WIN1252 DEFAULT 'A' NOT NULL COLLATE WIN_PTBR,
  DT_CADAST Date DEFAULT CURRENT_DATE NOT NULL,
  HR_CADAST Time DEFAULT CURRENT_TIME
 NOT NULL,
  FRACIONADO Char(1) CHARACTER SET WIN1252 DEFAULT 'N' NOT NULL COLLATE WIN_PTBR,
  PRC_VENDA Numeric(18,4) DEFAULT 0.01 NOT NULL,
  PRC_CUSTO Numeric(18,4),
  ULT_VENDA Date,
  MARGEM_LB Numeric(18,4),
  POR_COMISSAO Numeric(18,4),
  ULT_FORNEC Integer,
  GRADE_SERIE Char(1) CHARACTER SET WIN1252 DEFAULT 'N' COLLATE WIN_PTBR,
  ID_TIPOITEM Varchar(2) DEFAULT 0 NOT NULL,
  ID_CTI Char(3),
  CST_PIS Varchar(3) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  CST_COFINS Varchar(3) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  PIS Numeric(18,4),
  COFINS Numeric(18,4),
  UNI_MEDIDA Varchar(3),
  MARGEM_PV Numeric(18,4),
  CFOP Varchar(4) DEFAULT '5102',
  OBSERVACAO Blob sub_type 1 CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  NAT_RECEITA Smallint,
  CFOP_NF Varchar(4),
  PRC_ATACADO Numeric(18,4),
  ID_CTI_PART Char(3),
  ID_CTI_FCP Char(3),
  QTD_ATACADO Numeric(18,4), +++
  CONSTRAINT TB_ESTOQUE_PK PRIMARY KEY (ID_ESTOQUE)
);

CREATE TABLE TB_EST_PRODUTO
(
  ID_IDENTIFICADOR Integer NOT NULL,
  DESC_CMPL Varchar(30) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  COD_BARRA Varchar(18) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  REFERENCIA Varchar(18) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  PRC_MEDIO Numeric(18,4),
  QTD_COMPRA Numeric(18,4),
  QTD_ATUAL Numeric(18,4) NOT NULL,
  QTD_MINIM Numeric(18,4),
  QTD_INICIO Numeric(18,4),
  QTD_RESERV Numeric(18,4),
  QTD_POSVEN Numeric(18,4),
  ULT_COMPRA Date,
  PESO Numeric(18,4),
  IPI Numeric(18,4),
  CF Varchar(13) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  IAT Char(1) CHARACTER SET WIN1252 DEFAULT 'A' COLLATE WIN_PTBR,
  IPPT Char(1) CHARACTER SET WIN1252 DEFAULT 'T' COLLATE WIN_PTBR,
  COD_NCM Varchar(8) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  ID_NIVEL1 Smallint,
  ID_NIVEL2 Smallint,
  MVA Numeric(18,4),
  CST_IPI Varchar(3) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  FOTO Blob sub_type 0,
  CSOSN Varchar(3),
  ANP Integer,
  EXTIPI Smallint,
  CST Varchar(3),
  FCI Varchar(36),
  COD_CEST Varchar(7) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  CENQ Varchar(3) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  VLR_IPI Numeric(18,4), +++
  CST_CFE Varchar(3), +++
  CSOSN_CFE Varchar(3), +++
  CONTROLA_LOTE_VENDA Char(1) DEFAULT 'N' NOT NULL, +++
  BAIXA_LOTE_NFV Char(1) DEFAULT 'A', +++
  BAIXA_LOTE_PDV Char(1) DEFAULT 'A' +++
);

CREATE TABLE TB_IFS
(
  ID_IFS Smallint NOT NULL,
  CAIXA Varchar(6) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  LOJA Varchar(3) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  FABRICACAO Varchar(20) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  USUARIO Char(3) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  MARCA Varchar(20) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  MF Char(1) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  MODELO Varchar(20) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  TIPO Varchar(7) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  DATA_ON Date,
  DATA_OFF Date,
  ATIVO Char(1) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  ISS_RATEIO Char(1) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  SB_DATAIN Date,
  SB_HORAIN Time,
  SB_VERSAO Varchar(8) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  CHAVE Varchar(32) CHARACTER SET WIN1252 COLLATE WIN_PTBR,
  COD_NAC Varchar(10),
  DATA_TEMP Date,
  NUM_CREDENCIAMENTO Varchar(30), +++
  CONSTRAINT TB_IFS_PK PRIMARY KEY (ID_IFS)
);

CREATE TABLE TB_UNI_MEDIDA
(
  UNIDADE Varchar(3) NOT NULL,
  DESCRICAO Varchar(30) CHARACTER SET WIN1252 NOT NULL COLLATE WIN_PTBR,
  CONVERSOR Numeric(18,4) DEFAULT 1 NOT NULL,
  STATUS Char(1) DEFAULT 'A' NOT NULL,
  UNIDADE_EX Varchar(6), +++
  CONSTRAINT PK_TB_UNI_MEDIDA PRIMARY KEY (UNIDADE)
);